<nav class="navbar navbar-expand-lg navbar-light px-5 pt-4">
  <div class="container-fluid">
    <a class="dp-subtitle text-dark" href="<%= root_path %>">Home</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Profile
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href="#">Settings</a></li>
              <li><hr class="dropdown-divider"></li>
              <li class="d-flex justify-content-center align-items-center">
                <%= link_to destroy_user_session_url, { method: :delete } do %>
                  <button type="button" class="btn btn-dark">Sign out</button>
                <% end %>
              </li>
            </ul>
          </li>
          <% unless controller_name == "news" && action_name == "new" %>
            <li class="nav-item ms-3">
              <%= link_to new_news_url do %>
                <button type="button" class="btn btn-dark">Create publication</button>
              <% end %>
            </li>
          <% end %>
        <% else %>
          <li class="nav-item me-4">
            <a href="<%= new_user_registration_url %>">
              <button type="button" class="btn btn-dark">Create account</button>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= new_user_session_url %>">
              Login
            </a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>